# Next.js Example

Planned example app for `@domcraft/react`.

The package itself should stay framework-agnostic. This example will show how to import the CSS and use `CursorDrone` in a client component.
